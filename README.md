# File-Handling-System
A simple Python application that demonstrates file handling operations such as writing, reading, and appending data using a text file. The program takes user input, stores it in a file, allows additional data to be added, and retrieves stored content, showcasing core concepts like file modes, functions, and exception handling
